#!/usr/bin/python3
import sys

print("testing tensorflow in python3")

try:
    import tensorflow as tf
    hello = tf.constant('It works!')
    sess = tf.Session()
    print(sess.run(hello))
except Exception as e:
    print("Doesn't work!: "+str(e.msg))
